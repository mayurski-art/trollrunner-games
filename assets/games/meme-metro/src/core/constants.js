// Shared gameplay constants for Troll Dash: Meme Metro.

// X positions of the three lanes (world units).
export const LANES = [-2.2, 0, 2.2];

// Obstacles/coins spawn this far ahead of the player (negative z = forward).
export const SPAWN_Z = -150;

// Objects past this z (behind the camera) are recycled.
export const DESPAWN_Z = 16;
